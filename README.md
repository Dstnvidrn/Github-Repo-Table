# Github-Repo-Table
This application will search for a username on github using the github repos API and return the Repo names, ids, and URLs in a table format.
The table can be sorted by name or id in ascending/decending order.


## Features to come:
<ul>
  <li>Displaying message on failed search</li>
</ul>
